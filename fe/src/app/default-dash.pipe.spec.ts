import { DefaultDashPipe } from './default-dash.pipe';

describe('DefaultDashPipe', () => {
  it('create an instance', () => {
    const pipe = new DefaultDashPipe();
    expect(pipe).toBeTruthy();
  });
});
